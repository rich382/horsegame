# 2026-08-17 — Bigger farm, drive off the arena

## Ask

Truck and trailer spawned, but the drive sat on the arena. Expand the lot so the driveway has room of its own.

## What shipped

- Grass and pick-ground are **160 × 160**.
- Arena stays east of the barn (`x = 6.5`).
- Drive pad at `x = −42` (14 × 26) plus a gravel lane along the barn row to the west entrance.
- Truck / trailer park on that pad. Paddock rails keep turnout off the lane.
- Perimeter trees. Orbit camera starts further out (38) and zooms to 110.

## Tests

`test_barn_business` asserts `Drive` exists and `position.x <= -32`.
