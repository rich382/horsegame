# 2026-08-16 — Jumper resolver + home gym recap

## Goal

Next slice beat after school: the same fence math Saturday will use, visible at home.

## What

- `ShowResolver` / `JumperJudge` with published oracles A–D (`tests/test_jumper_judge.gd`).
- Home gymnastic (`home_gym_080`, 4 efforts) calls `resolve_trip` on the sim rng **before** the session skill bump.
- Recap card: each fence, faults, a short comment. Footing 40 vs 65 matches the design contrast on Example A.

## Still open

Ashford prize list, haul, entry, 7 NPC field (PR 7b). Course theater (7c).
